#ifndef LINUX_EVDEV_UTILS_H
#define LINUX_EVDEV_UTILS_H

const char* button_code_text(int virtualCode);
const char* abs_code_text(int virtualCode);
const char* rel_code_text(int virtualCode);

#endif
